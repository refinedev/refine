apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "readmin-example.fullname" . }}-test-connection"
  labels:
    {{- include "readmin-example.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "readmin-example.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
