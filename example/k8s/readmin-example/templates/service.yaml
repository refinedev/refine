apiVersion: v1
kind: Service
metadata:
  name: {{ include "readmin-example.fullname" . }}
  labels:
    {{- include "readmin-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "readmin-example.selectorLabels" . | nindent 4 }}
