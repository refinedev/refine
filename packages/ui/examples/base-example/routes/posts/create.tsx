import {
  CreateView,
  CreateViewContent,
  CreateViewHeader,
} from "@/registry/default/refine-ui/views/create-view";

export default function CreatePost() {
  return (
    <CreateView>
      <CreateViewHeader />
      <CreateViewContent>CreatePost</CreateViewContent>
    </CreateView>
  );
}
