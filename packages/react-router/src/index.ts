import {
    useHistory,
    useLocation,
    useParams,
    BrowserRouter,
    Switch,
    Route,
    Prompt,
    Link,
    Redirect,
} from "react-router-dom";

const RouterProvider = () => ({
    useHistory,
    useLocation,
    useParams,
    BrowserRouter,
    Switch,
    Route,
    Prompt,
    Link,
    Redirect,
});

export default RouterProvider;
