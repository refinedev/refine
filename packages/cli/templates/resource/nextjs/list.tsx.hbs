import { IResourceComponentsProps, GetListResponse, useMany } from "@pankod/refine-core";
import { {{capitalize uiFramework}}ListInferencer } from "@pankod/refine-inferencer/{{uiFramework}}";

export const {{capitalize resource}}List: React.FC<IResourceComponentsProps<GetListResponse<any>>> = ({ initialData }) => {
    return <{{capitalize uiFramework}}ListInferencer />;
};
