import { IResourceComponentsProps, GetListResponse, useMany } from "@pankod/refine-core";
import { } from "@pankod/refine-{{uiFramework}}";

export const PostList: React.FC<IResourceComponentsProps<GetListResponse<any>>> = ({ initialData }) => {
    return <span>List Page</span>
};