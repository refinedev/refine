import { IResourceComponentsProps, GetListResponse } from "@refinedev/core";
{{#if uiFramework}}
import { {{formatInferencerComponent uiFramework}}Inferencer } from "@refinedev/inferencer/{{uiFramework}}";
{{/if}}

export const {{capitalize resource}}List: React.FC<IResourceComponentsProps<GetListResponse<{}>>> = () => {
    {{#if uiFramework}}
    return <{{formatInferencerComponent uiFramework}}Inferencer />;
    {{else}}
    return <span>ListPage</span>;
    {{/if}}
};
