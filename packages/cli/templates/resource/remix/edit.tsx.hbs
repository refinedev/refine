import { IResourceComponentsProps } from "@pankod/refine-core";
import { {{capitalize uiFramework}}EditInferencer } from "@pankod/refine-inferencer/{{uiFramework}}";


export const {{capitalize resource}}Edit: React.FC<IResourceComponentsProps> = () => {
    return <{{capitalize uiFramework}}EditInferencer />;
};
