import { IResourceComponentsProps } from "@pankod/refine-core";
import { {{capitalize uiFramework}}ShowInferencer } from "@pankod/refine-inferencer/{{uiFramework}}";


export const {{capitalize resource}}Show: React.FC<IResourceComponentsProps> = () => {    
    return <{{capitalize uiFramework}}ShowInferencer />;
};
