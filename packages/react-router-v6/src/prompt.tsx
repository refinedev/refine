import { UNSAFE_NavigationContext } from "react-router-dom";
import { PromptProps } from "@pankod/refine-core";

export const Prompt: React.FC<PromptProps> = ({
    message,
    setWarnWhen,
    when,
}) => {
    return null;
};
