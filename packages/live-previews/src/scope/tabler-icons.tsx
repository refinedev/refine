import { IconSun, IconMoonStars, IconLanguage } from "@tabler/icons";

const TablerScope = {
    TablerIcons: {
        IconSun,
        IconMoonStars,
        IconLanguage,
    },
};

export default TablerScope;
