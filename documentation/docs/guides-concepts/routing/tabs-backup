<Tabs>
<TabItem value="without-route-definition" label="Without route definiton">

```diff
import { Refine, useList } from "@refinedev/core";

export const App = () => {
    <Refine resources={[
            {
                name: "products"
            },
        ]}
    >
        {/* ... */}
    </Refine>
}

// http://localhost:5173/my-products
export const ProductList = () => {
    // highlight-next-line
    const { data } = useList({ resource: "products" })
    // ...
}

// http://localhost:5173/my-products/123
export const ProductShow = () => {
    // highlight-next-line
    const { queryResult: { data } } = useShow({ resource: "products", id: "123" })
    // ...
}
```

</TabItem>

<TabItem value="with-route-definition" label="With route definition">

```tsx
import { Refine, useList } from "@refinedev/core";

export const App = () => {
    <Refine resources={[
            {
                name: "products",
                // highlight-next-line
                list: "/my-products",
                // highlight-next-line
                show: "/my-products/:id",
            },
        ]}
    >
        {/* ... */}
    </Refine>
}

// http://localhost:5173/my-products
export const ProductList = () => {
    // highlight-next-line
    const { data } = useList()
    // ...
}

// http://localhost:5173/my-products/123
export const ProductShow = () => {
    // highlight-next-line
    const { queryResult: { data } } = useShow()
    // ...
}
```

</TabItem>
</Tabs>
